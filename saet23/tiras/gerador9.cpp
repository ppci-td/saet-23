#include <bits/stdc++.h>

using namespace std;

#define MAX_N 100000
#define MAX_C 100000

int main() {
    srand(time(NULL) + clock());

    int n = MAX_N;

    printf("%d\n", n);
    printf("99991");
    for(int i = 0; i<n; ++i) {
        printf(" 99991");
    }
    printf("\n");

    return 0;
}
